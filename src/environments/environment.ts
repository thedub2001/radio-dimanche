// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase : {
  apiKey: "AIzaSyCkhsnx2M_PpiqwLpttOHimiKwll-ncRo4",
  authDomain: "radio-dimanche.firebaseapp.com",
  databaseURL: "https://radio-dimanche-default-rtdb.europe-west1.firebasedatabase.app",
  projectId: "radio-dimanche",
  storageBucket: "radio-dimanche.appspot.com",
  messagingSenderId: "222599428583",
  appId: "1:222599428583:web:5d1baadcf10daead57a542"
}
};

const firebaseConfig = {

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
